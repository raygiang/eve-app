import React from 'react';
import { WordList } from '../../models/models';

interface ListContainerProps {
  words: WordList,
}

const ListContainer = ({ words }: ListContainerProps): JSX.Element => {
  return (
    <div>
      
    </div>
  )
}

export default ListContainer;
